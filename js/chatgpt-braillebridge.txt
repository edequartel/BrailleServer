<script src="../js/braillebridge.js"></script>
<script>
  // Basic usage
  BrailleBridge.setConfig({
    baseUrl: "http://localhost:5000",
    wsUrl: "ws://localhost:5000/ws",
    displayCells: 40,
    debug: true
  });

  BrailleBridge.on("connected", () => {
    console.log("Bridge connected!");
  });

  BrailleBridge.on("cursor", evt => {
    console.log("Cursor at index:", evt.index, "raw:", evt.raw);
  });

  BrailleBridge.on("thumbkey", evt => {
    console.log("Thumbkey:", evt.name, "lower:", evt.nameLower);
  });

  BrailleBridge.connect();

  // Send text to braille:
  function testSend() {
    BrailleBridge.sendText("Hallo braille!");
  }
</script>


// usage
BrailleBridge.sendText("some text");
BrailleBridge.on("cursor", evt => handleCursor(evt.index));
BrailleBridge.on("thumbkey", evt => handleThumb(evt.name));
